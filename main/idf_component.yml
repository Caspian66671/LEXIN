dependencies:
  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  espressif/esp_hosted:
    version: "~2"
    rules:
      - if: "target in [esp32p4, esp32h2]"
  esp_lcd_ek79007: "^1.0.0"
  esp_lcd_touch_gt911: "^1.1.3"
  espressif/esp_lvgl_port: "^2.6.0"
  idf: "^5.0"
